<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 1200px;
            height: 630px;
            background-color: #272523;
            font-family: 'IBM Plex Mono', monospace;
            display: flex;
            flex-direction: column;
            padding: 80px 80px 60px 80px;
        }

        .accent-line {
            width: 120px;
            height: 4px;
            background-color: #007bff;
            margin-bottom: 32px;
        }

        .title-container {
            flex: 1;
            display: flex;
            align-items: flex-start;
        }

        h1 {
            font-weight: 700;
            line-height: 1.2;
            letter-spacing: -0.02em;
            color: #f5f5f5;
            margin: 0;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        h1.short {
            font-size: 72px;
        }

        h1.medium {
            font-size: 56px;
        }

        h1.long {
            font-size: 44px;
        }

        .site-name {
            font-size: 24px;
            font-weight: 400;
            color: #c0c0c0;
            margin-top: auto;
        }
    </style>
</head>
<body>
    <div class="accent-line"></div>
    <div class="title-container">
        <h1 class="{% if title | length < 40 %}short{% elif title | length < 70 %}medium{% else %}long{% endif %}">{{ title }}</h1>
    </div>
    <div class="site-name">disnetdev.com</div>
</body>
</html>
